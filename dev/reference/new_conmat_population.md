# Create a new `conmat_population` class object

Create a new `conmat_population` class object

## Usage

``` r
new_conmat_population(data, age, population)
```

## Arguments

- data:

  data.frame

- age:

  bare column name of numeric data on age

- population:

  bare column name of numeric data on population

## Value

object with class `conmat_population`
